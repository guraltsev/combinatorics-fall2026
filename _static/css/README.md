# Theme CSS

`sphinx-theme.css` is the course-wide public theme override; `tippy.css` styles local hover previews. Keep selectors synchronized with templates and extension-emitted classes, preserve responsive and reduced-JavaScript behavior, and verify changed presentation in a local preview. See the parent [_theme README](../../README.md) for builder placement and safety rules.
