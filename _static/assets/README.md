# Public theme icons

These local SVG files provide the icons referenced by semantic callout and navigation presentation. They are emitted as public static assets, not generated files. Keep filenames synchronized with `_extensions/course_roles.py`, templates, and `sphinx-theme.css`; preserve accessible SVG metadata when replacing an icon. Verify a changed icon in normal and dark-theme preview.
