# Theme JavaScript

Each file progressively enhances a specific rendered feature: focused sections, overflowing navigation, page TOC state, search results, sidebar collapse, MathJax enhancement, MyOpenMath exports, or heading matches. The HTML/CSS baseline remains authoritative. Keep data attributes and class names synchronized with templates and extension output; verify math changes with `npm run test:math`. See the parent [_theme README](../../README.md).
